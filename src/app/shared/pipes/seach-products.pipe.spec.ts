import { SeachProductsPipe } from './seach-products.pipe';

describe('SeachProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new SeachProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
