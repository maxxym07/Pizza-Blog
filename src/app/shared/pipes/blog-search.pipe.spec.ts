import { BlogSearchPipe } from './blog-search.pipe';

describe('BlogSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new BlogSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
